services:
  alpha-fusion:
    build: .
    container_name: alpha-fusion
    restart: unless-stopped
    env_file:
      - /srv/secrets/alpha-fusion.env
    ports:
      - "127.0.0.1:8007:8007"
    networks:
      - infra-net

networks:
  infra-net:
    external: true
